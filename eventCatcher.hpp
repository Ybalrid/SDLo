#ifndef eventCatcher
#define eventCatcher
#include <SDL.h>
#include <iostream>

namespace SDLo
{

    class EventCatcher
    {
        public:
            EventCatcher();
            ~EventCatcher();
        protected:
        private:

    };
}

#endif
